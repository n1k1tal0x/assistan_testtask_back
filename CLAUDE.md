# Правила репозитория

- Не коммить изменения сразу после их внесения. Делать `git commit` только когда пользователь
  явно попросит об этом (например, «закомить»/«commit»). До этого изменения должны оставаться
  в рабочем дереве незакоммиченными.
